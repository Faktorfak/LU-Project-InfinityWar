using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Aliver : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Boss2.isAlive = true;
        BossHealth.bossIsAlive = true;
    }

    
}
